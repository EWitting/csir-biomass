defaults:
  - base_cv
  - hydra/logging
  - model: biomass_sliding_window # Add models or ensembles
  - wandb: train

###############
#             #
#    PATHS    #
#             #
###############
processed_path: data/processed
raw_path: data/raw
cache_path: data/cache
data_path: data/raw/train.csv

scorer:
  _target_: src.scoring.global_weighted_r2.GlobalWeightedR2
  name: global_weighted_r2  # New global metric
  verbose: true
  log_old_metric: true  # Also log old "weighted_r2" for comparison
  metadata_path: ${data_path}  # Will load metadata from train.csv

splitter:
  _target_: sklearn.model_selection.KFold
  n_splits: 5
  shuffle: true
  random_state: 42
allow_multiple_instances: true
save_folds: true
